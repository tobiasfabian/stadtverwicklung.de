title: Blog

navigation:
  status: all
  template: all

tabs:
  content:
    label: Inhalt
    icon: page
    sections:
      pages:
        headline: Blog-Einträge
        info: "{{ page.date.toDate('d.m.Y') }}"
        flip: true
        templates:
          - blog-entry
  meta:
    extends: tabs/meta
  analytics:
    extends: tabs/analytics
