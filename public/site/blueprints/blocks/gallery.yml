name: field.blocks.gallery.name
icon: dashboard
preview: gallery
fields:
  items:
    type: structure
    fields:
      image:
        label: field.blocks.image.name
        type: files
        query: model.images
        multiple: false
        layout: cards
        size: small
        required: true
        image:
          ratio: 3/2
      caption:
        label: field.blocks.image.caption
        extends: fields/writer-inline
  ratio:
    extends: fields/ratio
    width: 1/2
    required: true
    placeholder: false
    default: 3/2
  maxWidth: fields/max-width
